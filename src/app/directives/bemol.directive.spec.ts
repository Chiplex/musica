import { BemolDirective } from './bemol.directive';

describe('BemolDirective', () => {
  it('should create an instance', () => {
    const directive = new BemolDirective();
    expect(directive).toBeTruthy();
  });
});
