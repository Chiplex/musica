import { AcordeDirective } from './acorde.directive';

describe('AcordeDirective', () => {
  it('should create an instance', () => {
    const directive = new AcordeDirective();
    expect(directive).toBeTruthy();
  });
});
