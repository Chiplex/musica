import { ParlanteDirective } from './parlante.directive';

describe('ParlanteDirective', () => {
  it('should create an instance', () => {
    const directive = new ParlanteDirective();
    expect(directive).toBeTruthy();
  });
});
